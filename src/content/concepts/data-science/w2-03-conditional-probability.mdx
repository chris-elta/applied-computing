---
course: data-science
section: w2-probability
order: 180
title: Conditional Probability
visuals:
  - type: svg
    src: data-science/w2-probability/05.svg
    caption: Slide 5 of 23 — the definition, and what it becomes when the events are independent.
  - type: animation
    component: DiceGrid
    caption: The 36 outcomes of two dice, stepping from P(A) and P(B) to P(A ∩ B) and finally P(A|B).
notation:
  - symbol: "P(A|B)"
    reads: P of A given B
    means: >-
      The probability of `A` once you already know `B` happened. The bar reads "given", not
      "divided by".
  - symbol: "P(A ∩ B) / P(B)"
    reads: P of A and B, over P of B
    means: >-
      The definition of `P(A|B)`: of all the ways `B` can happen, the share in which `A`
      happens too.
questions:
  - ask: >-
      If `A` and `B` are independent, what is `P(A|B)`?
    choices:
      - text: "`P(A)` — knowing `B` changes nothing"
        correct: true
        explains: >-
          By the definition, `P(A|B) = P(A ∩ B) / P(B)`. With independence, the top is
          `P(A) × P(B)`, and the `P(B)` cancels. This is what the slide means by
          conditional probabilities getting interesting only when events are not
          independent.
      - text: "`P(B)`"
        explains: >-
          That would be `P(B|A)` if `B` did not depend on `A`, but here we are asking about
          `A`. The result is the probability of `A`.
      - text: "`0`"
        explains: >-
          Zero would mean `A` cannot happen when `B` does. Independence says `B` has no
          effect, which is a different thing.
      - text: "`P(A) × P(B)`"
        explains: >-
          That is the probability of *both* happening, `P(A ∩ B)`. Conditioning divides by
          `P(B)`, which removes the `P(B)` factor.
    work: |-
      Definition: `P(A|B) = P(A ∩ B) / P(B)`.
      Independence: `P(A ∩ B) = P(A) × P(B)`.
      Substitute: `P(A|B) = P(A) × P(B) / P(B) = P(A)`.
      So conditioning on `B` changes nothing. `P(A) × P(B)` is the joint probability, before dividing by `P(B)`.
assessed:
  - "Lab 1 · Task 6"
prerequisites:
  - topic: "Probability by counting"
    explains: "When every row is equally likely to be picked, `P(event)` is the number of rows in the event divided by the total number of rows. A two-by-two table of counts, with row and column totals, holds every probability in this concept."
    see: "data-science/w2-01-probability-basics"
  - topic: "Complement"
    explains: "`not A` means everything outside `A`, so `P(not A) = 1 − P(A)`. Splitting a table into `A` and `not A` covers every row exactly once."
---

You roll two dice and are told the first is a 6. How likely is it that the total is 10 or
more? The answer changed the moment you were told something, because you are no longer asking
about all rolls, only those that start with a 6.

## The definition

The **conditional probability** of `A` given `B` is

```
P(A|B) = P(A ∩ B) / P(B)
```

Read the fraction as a share. Of everything that can happen when `B` occurs (the bottom),
what portion also has `A` (the top)? Conditioning shrinks the world to the cases where `B`
happened and asks how much of that smaller world is `A`.

## Worked example: the dice

Roll two fair dice: 36 equally likely outcomes `(first, second)`. Let `A` be "the total is 10
or more" and `B` be "the first die is a 6".

Where does 36 come from? Each die has 6 faces and the first die does not limit the second, so
there are `6 × 6 = 36` combined outcomes. Picture a grid with the first die down the side and
the second across the top: 6 rows by 6 columns, one cell per outcome. Order counts, so (2,5)
and (5,2) are different cells. "The first die is a 6" is the sixth row, which is why
`P(B) = 6/36`.

1. **`P(B)`.** The outcomes with a 6 first are (6,1) to (6,6). That is 6 outcomes, so
   `P(B) = 6/36`.
2. **`P(A ∩ B)`.** Among those six, the total is at least 10 for (6,4), (6,5) and (6,6). That
   is 3 outcomes, so `P(A ∩ B) = 3/36`.
3. **Apply the definition.**

```
P(A|B) = P(A ∩ B) / P(B) = (3/36) / (6/36) = 3/6 = 0.5
```

The 36s cancel, which is why you can simply count inside the shrunken world: 6 outcomes
remain and 3 of them are in `A`.

Without the information, the outcomes with a total of 10 or more are (4,6), (5,5), (5,6),
(6,4), (6,5) and (6,6), so `P(A) = 6/36 ≈ 0.17`. Being told the first die is a 6 lifts it to
0.5.

Because `P(A|B) ≠ P(A)`, the events are **not independent**. Independence would need
`P(A ∩ B) = P(A) × P(B) = 1/36`, but the actual value is `3/36`.

## When it does nothing

The slide notes that conditional probabilities get interesting **only when events are not
independent**. If they are, `P(A ∩ B) = P(A) × P(B)`, and the definition collapses:

```
P(A|B) = P(A) × P(B) / P(B) = P(A)
```

Learning `B` leaves the probability of `A` where it was. This is the same fact as the
definition of independence, seen from the other side: independent means being told about
`B` is useless.

## Order matters

`P(A|B)` and `P(B|A)` are generally **different numbers**. The chance that a professional
footballer is fit is not the chance that a fit person is a professional footballer. The next
concept, Bayes' theorem, is exactly the rule that converts one into the other.

> Conditioning restricts attention to the cases where you know something happened. The
> bar means "given", and the two orders are different questions.
