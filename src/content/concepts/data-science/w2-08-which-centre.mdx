---
course: data-science
section: w2-descriptive
order: 230
title: Mean, Median, Geometric Mean — Which to Use
highlight: true
assessed:
  - "Lab 1 · Task 3"
prerequisites:
  - topic: "Skew"
    explains: "A distribution is *right-skewed* when a few very large values stretch its right tail. Those values pull the mean upward but barely move the median, so a mean well above the median is the sign of a right skew."
  - topic: "Geometric mean"
    explains: "The geometric mean multiplies the `n` values and takes the `n`th root. Equivalently, take the mean of the logarithms and undo it with an exponential. Because logarithms are undefined for zero and negative numbers, every value must be strictly positive. It suits quantities that combine by multiplying, such as growth rates."
visuals:
  - type: svg
    src: data-science/w2-probability/13.svg
    caption: Slides 13–15 of 23 — the mean, the alternatives, and the comparison table.
  - type: svg
    src: data-science/w2-probability/14.svg
  - type: svg
    src: data-science/w2-probability/15.svg
notation:
  - symbol: mean
    reads: mean, or arithmetic mean
    means: >-
      Add all the values and divide by how many there are.
  - symbol: median
    reads: median
    means: >-
      The middle value once the data is sorted. Half the values lie below it and half above.
  - symbol: geometric mean
    reads: geometric mean
    means: >-
      The `n`th root of the product of `n` values. It averages *multipliers*, such as growth
      factors, rather than amounts.
questions:
  - ask: >-
      Salaries are 50k, 50k, 60k, 70k and 5M. Which summary best represents a typical
      salary?
    choices:
      - text: The median, 60k
        correct: true
        explains: >-
          The one enormous value drags the mean to about 1.05M, higher than every salary but
          one. The median depends only on the middle of the sorted list, so an outlier cannot
          distort it.
      - text: The mean, about 1.05M
        explains: >-
          The mean uses every value, which is its strength for symmetric data and its
          weakness here. A single outlier makes it unrepresentative.
      - text: The geometric mean
        explains: >-
          It is meant for ratios and growth rates, not for amounts like salaries.
      - text: They are all equally good
        explains: >-
          They give 60k versus over a million on the same data, so the choice clearly
          matters.
    work: |-
      Mean: `(50 + 50 + 60 + 70 + 5000) / 5 = 5230 / 5 = 1046`, about 1.05M.
      That is higher than four of the five salaries, so it is not typical.
      Median: sort `50, 50, 60, 70, 5000`. The middle value is 60k.
      The median ignores how big the outlier is, so it is the better summary. The geometric mean is for growth ratios, not amounts.
  - ask: >-
      A stock rises 50% one year and falls 50% the next. What is its average yearly change?
    choices:
      - text: >-
          About −13% a year: 1.5 × 0.5 = 0.75, and the geometric mean of the two factors is
          the square root of 0.75, about 0.87
        correct: true
        explains: >-
          Starting at 100 you reach 150 and then 75, a loss overall. The arithmetic mean says
          0%, which claims no change and is wrong. Growth compounds by multiplication, so the
          geometric mean is right.
      - text: 0%, the arithmetic mean of +50% and −50%
        explains: >-
          If it were 0% a year you would end where you began, but you end at 75.
      - text: +25%
        explains: >-
          This ignores that the fall applies to a bigger base than the rise. Percentages on
          different bases cannot simply be averaged.
      - text: −50%
        explains: >-
          That is the second year alone. Averaging must cover both.
    work: |-
      Convert each change to a factor: +50% is `1.5`, −50% is `0.5`.
      Start at 100. After year one: `100 × 1.5 = 150`. After year two: `150 × 0.5 = 75`.
      Overall factor: `1.5 × 0.5 = 0.75`, a 25% loss over two years.
      Geometric mean: `√0.75 ≈ 0.866`, so about `−13%` a year. Check: `0.866² ≈ 0.75`.
      The arithmetic mean says 0%, which would leave you at 100, not 75.
---

A summary of data hides something. The question is whether it hides the thing you care
about. The lecture gives three measures of centre and a table for choosing between them.

## The mean

Add the values and divide by how many there are. It uses all the data and is easy to compute.
The slide's caveat: the mean is meaningful for **symmetric distributions without outliers**.

## The median

The middle value once the data is sorted. It ignores how extreme the extremes are, which
protects it from outliers and makes it good for skewed data. The price is that it ignores
most of the exact values.

## The geometric mean

The `n`th root of the product of `n` values. It is always at most the arithmetic mean, and
more sensitive to values near zero. It suits **ratios**: 1/2 and 2/1 should average to 1, and
the geometric mean gives exactly `√(0.5 × 2) = 1`, where the arithmetic mean gives 1.25.

It cannot be used with negative values.

## Two worked cases

**Salaries: 50k, 50k, 60k, 70k, 5M.** The mean is 5,230k ÷ 5 ≈ 1.05M. No one earns anything
like that; four of five earn under 71k. The median is 60k, a much better "typical" salary.
An outlier controls the mean and barely touches the median.

**Growth: +50%, then −50%.** Think of them as multipliers, 1.5 and 0.5. Start with 100: you
reach 150, then 75. The arithmetic mean of the percentages is 0%, which says nothing changed,
yet you lost a quarter. The geometric mean of the multipliers is `√0.75 ≈ 0.866`, about
−13% per year, and `0.866² = 0.75` reproduces what happened.

**Three years: +10%, +20%, −10%.** The multipliers are 1.10, 1.20 and 0.90. Start with 100:
you reach 110, then 132, then 118.8.

- *Root of the product:* `1.10 × 1.20 × 0.90 = 1.188`, and the cube root is `1.188^(1/3) ≈ 1.059`.
- *Mean of the logarithms:* `ln 1.10 + ln 1.20 + ln 0.90 ≈ 0.0953 + 0.1823 − 0.1054 = 0.1723`.
  Divide by 3 to get 0.0574, and `e^0.0574 ≈ 1.059`. The same answer.

The typical growth is about **+5.9% a year**, and `1.059³ ≈ 1.188` reproduces the 118.8. The
arithmetic mean of the percentages, (10 + 20 − 10) ÷ 3 ≈ 6.7%, is too high: at 6.7% a year
you would reach about 121.4, not 118.8.

## Choosing

| Measure | Use it when | Avoid it when |
| --- | --- | --- |
| Mean | Data is roughly symmetric, no outliers | Skewed data or outliers |
| Median | Skewed data, outliers | You need every value to count |
| Geometric mean | Ratios, percentages, growth rates | Negative values |

## The misconception

"The average" is not one thing. A report that says "average salary" without saying which is
either careless or selective, and the difference can be a factor of twenty.

The measures also connect to spread. In the next concept the standard deviation is built
around the mean, so it inherits the same sensitivity to outliers.

> Ask what the data looks like before picking the average. Skew and outliers favour the
> median; growth favours the geometric mean.
