---
course: applied-algorithm-analysis
section: w1-review
order: 180
title: Where We Landed
visuals:
  - type: html
    src: /slides/applied-algorithm-analysis/week1.html?hash=true#/53
    caption: >-
      Slides 63–65 of 65 — the Part 1 summary and self-check.
notation:
  - symbol: "O · Ω · Θ · o"
    reads: big-oh, big-omega, big-theta, little-oh
    means: >-
      The four relations between functions: `≤`, `≥`, `=` and a strict `<`. Each needs
      witnesses — a constant `c` and a threshold `n₀` — before it counts as proved.
  - symbol: "witness"
    reads: witness
    means: >-
      The specific `c` and `n₀` that back a bound. Naming them is what turns an assertion
      into a proof.
questions:
  - ask: >-
      `fib3` makes the same recursive calls as `fib1`. Why is it linear anyway?
    choices:
      - text: Each memo entry is computed once; every repeat call returns in `O(1)`
        correct: true
        explains: >-
          There are only `n` distinct subproblems. The first visit to each does the work,
          and every later visit is a table lookup — so the total work is proportional to
          the number of distinct subproblems, not to the size of the call tree.
      - text: The memo makes the recursion depth logarithmic
        explains: >-
          The depth is still `n`, which is why `fib3` can overflow the stack where `fib2`
          cannot. Depth is not what changed.
      - text: It makes strictly fewer calls than `fib1`
        explains: >-
          It makes the same calls — that is the premise. What changed is how much work each
          call does.
      - text: Because addition is `O(1)`
        explains: >-
          Both versions assume that, and `fib1` is exponential regardless. The assumption
          is not what separates them.
  - ask: >-
      An algorithm is both `O(n)` and `Ω(n)`. What is the one-symbol conclusion — and what
      does it still not tell you?
    choices:
      - text: "`Θ(n)`; it says nothing about best vs worst case, or about constants"
        correct: true
        explains: >-
          Matching upper and lower bounds give `Θ`. But bound-tightness and case are
          independent axes, so `Θ(n)` alone does not say which case was analysed — and the
          constants it suppresses may still decide which implementation you ship.
      - text: "`Θ(n)`, and the algorithm's running time is therefore fully determined"
        explains: >-
          `Θ` pins the growth *order* only. Two `Θ(n)` algorithms can differ by a factor of
          100 in practice.
      - text: "`O(n)`, since the upper bound is what matters"
        explains: >-
          When both bounds hold, `Θ` is the stronger and more honest statement. Reporting
          only `O` throws away what you proved.
      - text: Nothing — the two bounds are contradictory
        explains: >-
          They are complementary, not contradictory. Holding simultaneously is exactly the
          definition of `Θ`.
---

Three things to carry forward from Part 1, and one habit.

## 1 · The algorithm decides what is computable, not the machine

One problem, three algorithms, exponential down to linear — with the hardware never changing.
`fib1` multiplies its work by `φ ≈ 1.618` per extra `n`, so a machine twice as fast buys
about **1.4 more inputs**. `fib2` reaches `n = 200` in 199 additions on anything.

That gap is not an optimisation. It is the difference between possible and impossible.

## 2 · Asymptotic notation reports the shape, not the number

**Suppress constant factors and lower-order terms** — constants because they describe your
machine, lower-order terms because they stop mattering.

| | reads as | means |
| --- | --- | --- |
| `O` | `≤` | grows no faster than |
| `Ω` | `≥` | grows at least as fast as |
| `Θ` | `=` | grows at the same rate as |
| `o` | strict `<` | grows strictly slower than |

Two things to keep straight: a claim needs **witnesses** (an actual `c` and `n₀`) to be
proved rather than asserted; and bound-tightness is a *separate axis* from which case you
mean.

## 3 · A proof is a fence, a starting line, and a showing

`T(n)` is only a name for the wait. The claim is about its **shape**, and proving it means
building a fence the growth never breaks through: `c` says how steep, `n₀` says from when,
and after that it must hold forever. Upper bounds **inflate** smaller terms up to the leading
one; lower bounds **discard** them.

## The habit: name your witnesses

Every bound you assert should come with the specific `c` and `n₀` that back it. "Obviously
it is `O(n²)`" is not a proof; `c = 12, n₀ = 1` is.

## Next

**Week 2 — algorithms with numbers.** Everything so far quietly assumed one addition is one
step. That assumption is about to break, and the whole of next week is what happens when you
price arithmetic by the *digits* of the numbers rather than their value.

## Reading

- Dasgupta, Papadimitriou & Vazirani, *Algorithms* — chapter 0 for `fib1`/`fib2` and Big-O.
- Roughgarden, *Algorithms Illuminated, Part 1* — chapter 2 for asymptotic notation.
